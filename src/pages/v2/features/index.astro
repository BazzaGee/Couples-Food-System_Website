---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import features from '../../../data/v2/features.json';
import WaitlistV2 from '../../../components/v2/WaitlistV2.astro';
import WaitlistFormV2 from '../../../components/v2/WaitlistFormV2.astro';
---

<BaseLayout title="Features | Couples Food System V2" description="Discover how our dual-mode meal picker and intuitive features make planning, shopping, and cooking together a breeze." version="v2">
  <section class="py-20 max-w-6xl mx-auto px-6">
    <div class="text-center max-w-3xl mx-auto mb-16">
      <h1 class="text-4xl md:text-5xl font-semibold text-v2-on-surface tracking-tight mb-6">Designed for "We", not "You"</h1>
      <p class="text-xl text-v2-on-surface-variant">Two ways to find your meal, one perfect dinner. Discover how our features make planning, shopping, and cooking together effortless.</p>
    </div>

    <div class="space-y-8">
      {features.features.map((feature, index) => (
        <div class={`bg-v2-surface-container-lowest rounded-3xl border border-v2-outline-variant p-8 md:p-12 flex flex-col md:flex-row gap-8 items-center shadow-sm ${index % 2 === 1 ? 'md:flex-row-reverse' : ''}`}>
          <div class="flex-1">
            <h2 class="text-3xl font-semibold mb-4 tracking-tight flex items-center gap-3 text-v2-on-surface">
              {feature.title}
              {feature.badge && (
                <span class="bg-v2-primary/10 text-v2-primary text-xs px-2 py-1 rounded-full uppercase tracking-wider font-semibold">
                  {feature.badge}
                </span>
              )}
            </h2>
            <p class="text-lg text-v2-on-surface-variant mb-6">{feature.shortDescription}</p>
            <a href={feature.href} class="text-v2-primary font-medium hover:text-v2-primary/80 inline-flex items-center gap-1">
              Learn more &rarr;
            </a>
          </div>
          <div class="flex-1 w-full rounded-2xl aspect-video overflow-hidden border border-v2-outline-variant">
            <img src={`/images/v2/screenshot-${feature.id === 'dual-mode-meal-picker' ? 'dual-mode' : feature.id === 'adaptive-cooking' ? 'adaptive' : feature.id === 'shared-grocery-list' ? 'shopping' : feature.id === 'pantry-ai' ? 'pantry' : 'mealplan'}.webp`} alt="{feature.title} illustration" class="w-full h-full object-cover" decoding="async" />
          </div>
        </div>
      ))}
    </div>
  </section>

  <WaitlistV2>
    <WaitlistFormV2 />
  </WaitlistV2>
</BaseLayout>
