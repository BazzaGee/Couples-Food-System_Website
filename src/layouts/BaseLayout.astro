---
import BaseHead from '../components/layout/BaseHead.astro';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  canonicalURL?: string;
  ogImage?: string;
  version?: 'v1' | 'v2';
}

const { title, description, canonicalURL, ogImage, version = 'v1' } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={title} description={description} canonicalURL={canonicalURL} ogImage={ogImage} version={version} />
  </head>
  <body class={`min-h-screen flex flex-col pt-20 ${version === 'v2' ? 'bg-v2-surface text-v2-on-surface' : ''}`}>
    <Header version={version} />
    <main class="flex-grow">
      <slot />
    </main>
    <Footer version={version} />
  </body>
</html>
